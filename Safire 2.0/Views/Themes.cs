
using System.Linq;
using MahApps.Metro;

namespace Safire.Views
{
    public sealed class Themes 
    {
		private static AppTheme currentTheme =ThemeManager.AppThemes.ElementAt(0);
        //private static Accent currentAccent = ThemeManager.DefaultAccents.First(x => x.Name == "Blue");
    }
}